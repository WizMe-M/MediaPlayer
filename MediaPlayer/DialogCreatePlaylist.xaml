<Window x:Class="MediaPlayer.DialogCreatePlaylist"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MediaPlayer"
        mc:Ignorable="d"
        Title="Создание плейлиста" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Text="Введите название для плейлиста:" FontSize="20"/>
        <TextBox Name="playlistNameTB" Grid.Row="1" MinWidth="250" Background="GhostWhite" FontSize="24"/>

        <Grid Grid.Row="2" Margin="5">
        <Button IsDefault="True" Click="Confirm_Click" MinWidth="60" Margin="0,0,155,0" >OK</Button>
        <Button IsCancel="True" MinWidth="60" Margin="155,0,0,0">Отмена</Button>
        </Grid>

    </Grid>
</Window>